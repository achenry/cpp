int main(){
    int n = 3 + 4; // operand, operator, operand, operator, operand

    // arithmetic operators
    int x = 4, y = 2;

    int z = x + y;
    z = x / y;
    z = x % y;
    z = --x;

    // assignment operators
    x += 5;
    y *= 2;

    // relational operators
    int x = 7;;
    bool a = x > 5;
    bool b = x < 4;

    // logical operators
    bool x = true, y = false;
    bool z = x && y;
    z = x || y;
    z = !x;

    // bitwise operations
    int x = 3; // in bits: 0011
    int y = 9; // in bits 1001

    int z = x << 2; // in bits: 1100
    z = y >> 1; // in bits: 0100
    z = x | y; // in bits 1011

    // operator procedure
    
}
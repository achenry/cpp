// What is the size of the array str in the following example:
// char str[] = "a short string";
// => sizeof(...) / sizeof(char)


// What is the length of "a short string"
// => +1 for null char, strlen(...)